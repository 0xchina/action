on: [push]

jobs:
  test_job:
    runs-on: ubuntu-latest
    name: test
    steps:
      - name: test
        id: test
        uses: 0xchina/action@master
        with:
          image: kknd0/testbig:latest
          cache-from: type=registry,ref=kknd0/testbig:buildcache
          cache-to: type=registry,ref=kknd0/testbig:buildcache,mode=max
